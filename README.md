# 🎮 Hangman Console Game

Консольная реализация классической игры **«Виселица»**, написанная на Java.  
Проект реализует полноценный игровой движок с поддержкой интерактивного и неинтерактивного режимов, модульной архитектурой и полным соответствием техническому заданию.

---

## 📌 Возможности

- Интерактивный режим с вводом букв пользователем  
- Неинтерактивный (тестовый) режим с аргументами командной строки  
- Случайный выбор слова, категории и уровня сложности  
- Гибкая настройка количества попыток  
- Пошаговая визуализация виселицы  
- Корректная обработка ошибок ввода  
- Разделение логики игры и пользовательского интерфейса  
- Поддержка автоматического тестирования  

---

## 🚀 Запуск программы

### Интерактивный режим

Запуск без аргументов:

```bash
java -jar hangman.jar
```

Программа автоматически:

- выбирает категорию и уровень сложности
- загадывает слово
- запускает игровой процесс


## Неинтерактивный (тестовый) режим
Запуск с аргументами командной строки:

```bash
java -jar hangman.jar <загаданное_слово> <введённые_буквы>
```

Формат вывода:
```bash
<состояние_слова>;<результат>
```

Пример:

```bash
java -jar hangman.jar apple aple
```

Результат выполнения:

```bash
a p p l e;WIN
```

## 🧱 Структура проекта
```
app-java/
├── .mvn
├── config
├── src
│   └── main
│       └── java
│           └── academy
│               ├── app
│               ├── config
│               ├── core
│               │   ├── eval
│               │   └── game
│               ├── infra
│               │   └── dict
│               │       ├── loader
│               │       ├── mapper
│               │       └── model
│               │           └── InMemoryDictionary
│               ├── ports
│               │   ├── dict
│               │   ├── io
│               │   └── render
│               └── ui
│                   └── interactive
└── resources
```


## 🧠 Архитектурные принципы

- Чёткое разделение бизнес-логики, инфраструктуры и пользовательского интерфейса
- Использование принципов Ports & Adapters (Hexagonal Architecture)
- Возможность расширения без изменения ядра
- Высокая тестируемость компонентов



## 🧪 Тестирование

Покрываются следующие сценарии:

- корректный выбор слова
- обновление состояния после каждого ввода
- нечувствительность к регистру
- корректная обработка некорректного ввода
- завершение игры при превышении лимита попыток
- корректная работа в неинтерактивном режиме


## 🛠 Используемые технологии

- Java
- Maven
- Стандартный ввод / вывод





